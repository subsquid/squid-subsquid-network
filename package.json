{
  "name": "squid-subsquid-network",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "biome check --fix --unsafe ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "~5.4.2"
  }
}
